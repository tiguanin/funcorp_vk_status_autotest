Для запуска проекта необходимо склонировать проект, если используется IntelliJ IDEA - Edit Configurations -> Add new configuration -> TestNG -> Test kind -> Suite -> Выбрать testng.xml из classpath.
